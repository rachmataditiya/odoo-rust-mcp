## Copy to .env (do not commit .env)
#
# =============================================================================
# Odoo 19+ (JSON-2 API with API Key)
# =============================================================================
# ODOO_URL=http://host.docker.internal:8069
# ODOO_DB=v19_pos
# ODOO_API_KEY=YOUR_API_KEY
#
# =============================================================================
# Odoo < 19 (JSON-RPC with Username/Password)
# =============================================================================
# Set ODOO_VERSION to your Odoo major version (e.g., "18", "17", "16")
# This enables legacy JSON-RPC authentication with username/password
#
ODOO_URL=http://host.docker.internal:8069
ODOO_DB=mydb
ODOO_VERSION=18
ODOO_USERNAME=admin
ODOO_PASSWORD=admin
#
# =============================================================================
# Optional settings
# =============================================================================
# ODOO_ENABLE_CLEANUP_TOOLS=true
# ODOO_TIMEOUT_MS=30000
# ODOO_MAX_RETRIES=2
#
# =============================================================================
# MCP Authentication (HTTP transport only)
# =============================================================================
# Set MCP_AUTH_TOKEN to enable Bearer token authentication for HTTP transport.
# Clients must include "Authorization: Bearer <token>" header in all requests.
# If not set, authentication is disabled (not recommended for production).
#
# MCP_AUTH_TOKEN=your-secure-random-token-here
#
# =============================================================================
# MCP tool/prompt config (IMPORTANT for stdio transport)
# =============================================================================
# When using stdio transport (e.g., Cursor), the binary runs from a different
# working directory, so you MUST use absolute paths for config files.
#
# Example (adjust path to your installation):
# MCP_TOOLS_JSON=/Users/yourname/path/to/odoo-rust-mcp/rust-mcp/config/tools.json
# MCP_PROMPTS_JSON=/Users/yourname/path/to/odoo-rust-mcp/rust-mcp/config/prompts.json
# MCP_SERVER_JSON=/Users/yourname/path/to/odoo-rust-mcp/rust-mcp/config/server.json
#
# For HTTP/WS transport or Docker, relative paths work fine:
# MCP_TOOLS_JSON=config/tools.json
# MCP_PROMPTS_JSON=config/prompts.json
# MCP_SERVER_JSON=config/server.json

